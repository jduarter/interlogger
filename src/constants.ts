export const CONSUMER_ANALYTICS_TYPE = Symbol('CONSUMER_ANALYTICS_TYPE');
export const CONSUMER_ERROR_REPORTER_TYPE = Symbol(
  'CONSUMER_ERROR_REPORTER_TYPE',
);

export enum LOG_LEVELS {
  error,
  warn,
  info,
  debug,
}
